async function request(url, getHeaders) {
    return await fetch(url, getHeaders);
}

function getHeaders(method = 'get', data) {
    const result = {
        method,
        headers: {}
    }

    if(data) {
        headers.
    }
}

const asd = fetch('', {
    method: 'get',
    headers: {

    },
    body
})